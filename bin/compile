#!/usr/bin/env bash
set -eo pipefail

echo "-----> Setting up Playwright"
export PLAYWRIGHT_BROWSERS_PATH="$HOME/.local-browsers"
playwright install
playwright install-deps

echo "-----> Buildpack compilation completed!"
